// This all all about understanding the layout of a project and different scriptings interacting together, throughout this excercise the user will jump through many files only changing snippits thus no source 
// Futhormore, some changes are done in editor so the source code would not change it ! O:

// more help below
/*

#3 ) spawn manager WorldObj change prefab: dog 2 ball
#4 ) player WorldObj: nothing to dog
#5 ) Edit Dog Box Collide : Editor Collider
#6 ) Destroy out of bouhnds script : max x = "dog<-30" and max y = "ball<-1" if either pass then destroy
#7 ) int ballIndex = Random.Range(0, 3); passed into arr index
#8 ) change value of "spawnInterval"

*/
